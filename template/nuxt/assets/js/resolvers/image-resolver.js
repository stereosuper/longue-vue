export const resolveSrc = ({ data: { url } }) => url;

// COMBAK: You'll need to resolve the input data and return the srcset attribute's string
export const resolveSrcSet = ({ data, breakpoints, isFullWidth }) => {
    // return srcsetOutput;
};

// COMBAK: You'll need to resolve the input data and return the sizes attribute's string
export const resolveSizes = ({ data, breakpoints, isFullWidth }) => {
    // return sizesOutput;
};

// COMBAK: You'll need to resolve the input data and return the commented structure
export const resolveInputData = data => {
    // return {
    //     url: ,
    //     dimensions: {
    //         width: ,
    //         height:
    //     },
    //     metadata: {
    //         alt: ,
    //         copyright:
    //     }
    // };
};
